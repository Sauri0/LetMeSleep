# Functional transition corrections — base 3e3c57f

This delta addresses the four findings in the animation review of d39417a. It changes no clips, meshes, materials, authority or input. The full ApplyAuthoritativeHands/arm solver region was compared as normalized source text against 9b214e8 and is identical.

1. Local interpolation requires currently eligible gait, not merely a configured presenter. Snapshot entry/exit and LateUpdate-only transitions (temporary actions and zero simulation delta) reset the interpolation baseline to current pose and invalidate gait contacts. Local strike/crouch/recovery/idle use current root position before the unchanged hands solver consumes current world targets. Temporary expiration is resolved before choosing root placement.
2. HumanViewCamera now runs at DefaultExecutionOrder1300, after body1100/facial1200. GameplayVisualPresenter supplies a callback to refresh CharacterView anchors immediately before the camera reads CameraEye; this avoids moving the camera late while still reading an old anchor copy. Clearing visuals also unbinds the camera callback. Director should inspect effective importer script order in Unity in case project overrides supersede the attribute.
3. EvaluateRenderedPose returns PoseOwner.Controller or PoseOwner.Locomotion. All internal suspension/cut/invalid-delta/generation-reset branches report Controller. Binding clears its usingLocomotion/cache and explicitly reapplies the actual selected controller action, bypassing gait redirection for that restoration. Repeated paused frames do not repeatedly restart an already restored state. The next eligible sample can reacquire the graph with a silent baseline. No extra Animator.Update call was introduced.
4. Audio disable unsubscribes contact callbacks but retains registered sources. Enable reattaches each live source with remove-then-add idempotency. Bind still forgets the old runtime's registrations; destruction/replacement still unregisters. Per-actor mode therefore persists across audio disable/enable without fallback footsteps or duplicate subscribers.

Offline verification: combined current Presentation runtime, Gameplay presentation source and new HumanLocomotionTransitionTests compile with zero errors/warnings against Unity6000.3.24f1. Pure clock suite rerun:21655 assertions passed, including16 cadence scenarios and resets. Compile is not Unity execution.

Director slot: run `LetMeSleep.Tests.PlayMode.HumanLocomotionTransitionTests` in Editor PlayMode. It uses temporary in-memory clips/controller/objects, creates no project assets, and covers explicit owner return for pause/cut/invalid speed, local strike/crouch pose cut and controller restoration, three audio disable/enable cycles with exactly one delegate, and final camera-anchor refresh/order. Tests were compiled here but NOT executed. Then run the actual four imported clips through moving→strike→crouch→recovery→moving, pause/resume, and audio re-enable while visuals persist. Compare current root/strike targets, final eye/camera position and actual audible contacts in the same rendered frames.

UI menus that merely block input are not simulation pauses; only zero simulation delta or explicit action eligibility drives the pause logic here. Remote strike interpolation retains its previous behavior. Native action pose evaluation before the hand solve and any final support drift remain inspection gates. G4 is still open; these source fixes are not native visual/auditory approval or a redesign.
