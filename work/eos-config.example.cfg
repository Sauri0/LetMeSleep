; EOS GameClient configuration. Copy to eos-config.local.cfg.
; Only Peer2Peer policy with UserRequired. Never use TrustedServer credentials.
; Not active in the game yet. No account passwords or backend tokens here.
[eos]
product_id="REEMPLAZAR_PRODUCT_ID"
sandbox_id="REEMPLAZAR_SANDBOX_ID"
deployment_id="REEMPLAZAR_DEPLOYMENT_ID"
client_id="REEMPLAZAR_GAMECLIENT_ID"
client_secret="REEMPLAZAR_GAMECLIENT_SECRET"
